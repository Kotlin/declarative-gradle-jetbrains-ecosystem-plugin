pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
    }
}

rootProject.name = "declarative-gradle-jetbrains-ecosystem-plugin"

include(":project-types:jvm-application")
